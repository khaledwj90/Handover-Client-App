import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    bold: {
        fontWeight: "800"
    },
    regular: {
        fontWeight: "400"
    },
    light: {
        fontWeight: "300"
    }
});

export default style
